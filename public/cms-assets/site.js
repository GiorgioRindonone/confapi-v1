(()=>{'use strict';let timer;const toast=message=>{const el=document.querySelector('#toast');el.textContent=message;el.classList.add('visible');clearTimeout(timer);timer=setTimeout(()=>el.classList.remove('visible'),5500);};const request=async(url,options={})=>{const headers={'x-csrf-token':document.querySelector('meta[name=csrf-token]')?.content||'',...options.headers};if(options.body&&!(options.body instanceof FormData)){headers['Content-Type']='application/json';options.body=JSON.stringify(options.body);}const response=await fetch(url,{...options,headers,credentials:'same-origin'});const type=response.headers.get('content-type')||'';const value=type.includes('application/json')?await response.json():await response.text();if(!response.ok){const error=new Error(value.error||'Operazione non riuscita. Controlla i dati e riprova.');error.status=response.status;throw error;}return value;};window.Confapi={toast,request};document.querySelector('.nav-toggle')?.addEventListener('click',e=>{const button=e.currentTarget,open=button.getAttribute('aria-expanded')!=='true';button.setAttribute('aria-expanded',String(open));document.querySelector('#primary-nav').classList.toggle('is-open',open);});document.addEventListener('click',e=>{document.querySelectorAll('.nav-dropdown[open]').forEach(d=>{if(!d.contains(e.target))d.open=false;});});document.addEventListener('keydown',e=>{if(e.key==='Escape')document.querySelectorAll('.nav-dropdown[open]').forEach(d=>d.open=false);});document.querySelectorAll('[data-print]').forEach(b=>b.addEventListener('click',()=>window.print()));document.querySelectorAll('[data-copy-link]').forEach(b=>b.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(location.href);toast('Link copiato. Puoi incollarlo in un messaggio.');}catch{toast('Copia l’indirizzo dalla barra del browser.');}}));})();
