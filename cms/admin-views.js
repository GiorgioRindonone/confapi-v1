const sql=require('./db');const {esc,icon,labels,date,layout,field}=require('./views');
const statusNames={draft:'Bozza',published:'Pubblicato',trash:'Nel cestino'};
function shell(user,title,body,active='dashboard'){return layout(title,`<main id="main" class="studio"><aside class="studio-sidebar"><a class="studio-brand" href="/admin">CONFAPI ROMA</a><div class="studio-label">REDAZIONE</div><nav class="studio-nav" aria-label="Gestione del sito">${[['dashboard','/admin','Panoramica'],['articles','/admin/articoli','Articoli'],['media','/admin/immagini','Immagini'],['services','/admin/servizi','Categorie dei servizi'],['leads','/admin/richieste','Richieste di contatto'],['memberships','/admin/adesioni','Domande di adesione'],['account','/admin/account','Accessi e account']].map(([id,url,label])=>`<a href="${url}" ${active===id?'aria-current="page"':''}>${label}</a>`).join('')}</nav><div class="studio-end"><a href="/" target="_blank" rel="noopener">Apri il sito ↗</a><form method="post" action="/admin/logout"><input type="hidden" name="_csrf" value="${esc(user.csrf)}"><button class="logout-button" type="submit">Esci dalla redazione</button></form></div></aside><div class="studio-main"><div class="studio-top"><span>CONFAPI Roma e ANIEM Lazio</span><span>${esc(user.name)} <span class="avatar">${esc(user.name.slice(0,2).toUpperCase())}</span></span></div>${body}</div></main>${mediaDialog()}`,{admin:true,csrf:user.csrf,scripts:['/cms-assets/vendor/quill.js','/cms-assets/admin.js']});}
function heading(title,subtitle='',button=''){return `<div class="studio-heading"><div><h1>${title}</h1>${subtitle?`<p>${subtitle}</p>`:''}</div>${button}</div>`;}
function mediaDialog(){return `<dialog id="media-dialog" class="media-dialog"><div class="dialog-heading"><div><h2>Scegli un’immagine</h2><p>Riutilizza una foto oppure caricane una nuova.</p></div><button type="button" class="row-button" data-close-media>Chiudi ×</button></div><div class="media-upload"><form id="media-upload-form"><label class="field">1. Seleziona la foto<input type="file" id="media-file" name="image" accept="image/jpeg,image/png,image/webp,image/gif" required></label><label class="field">2. Descrivi cosa si vede<input name="alt" id="media-alt" required minlength="3" maxlength="250" placeholder="Es. Incontro delle imprese associate nella sede di Roma"></label><button class="button" type="submit">Carica l’immagine ${icon('plus')}</button><p class="field-help">JPG, PNG, WebP o GIF, massimo 10 MB. Le immagini vengono ottimizzate automaticamente.</p><p class="form-error" role="alert"></p></form></div><div class="library-heading"><h3>Immagini già caricate</h3><input type="search" class="search-field" id="media-search" aria-label="Cerca immagini" placeholder="Cerca un’immagine…"></div><div id="media-library" class="media-library" aria-live="polite"></div></dialog>`;}
function login(error=''){return layout('Accesso alla redazione',`<main id="main" class="login-page"><a href="/"><img src="/assets/logos/logo-confapi_1000.png" alt="CONFAPI Roma"></a><form method="post" action="/admin/login" class="form-card"><div class="eyebrow">Area riservata</div><h1>Accedi alla redazione</h1><p>Gestisci articoli, immagini e richieste delle imprese.</p>${field('email','Email',{type:'email',required:true,autocomplete:'username',max:200})}${field('password','Password',{type:'password',required:true,autocomplete:'current-password',max:200})}${error?`<p class="form-error" role="alert">${esc(error)}</p>`:''}<button class="button" type="submit">Accedi ${icon()}</button><p class="field-help">Per recuperare l’accesso, contatta il responsabile del sito.</p></form><a class="text-link" href="/">← Torna al sito</a></main>`,{admin:true,robots:true});}
function articleRows(items){return `<div class="table-wrap"><table><thead><tr><th>Articolo</th><th>Dove compare</th><th>Stato</th><th>Ultima modifica</th><th>Apri</th></tr></thead><tbody>${items.map(a=>`<tr><td><a class="table-title" href="/admin/articoli/${esc(a.id)}">${esc(a.title)}</a>${a.services?.length?`<small>${a.services.map(s=>esc(s.name)).join(' · ')}</small>`:''}</td><td><span class="channel-label ${a.channel}">${labels[a.channel]}</span></td><td><span class="status ${a.status}">${statusNames[a.status]}</span></td><td>${date(a.updated_at)}</td><td><a class="row-button" href="/admin/articoli/${esc(a.id)}">Modifica</a></td></tr>`).join('')}</tbody></table></div>`;}
function dashboard(user){const counts=sql.get("SELECT COUNT(*) total,SUM(status='published') published,SUM(status='draft') draft FROM articles");return shell(user,'Panoramica',`${heading('Cosa vuoi pubblicare oggi?','Scegli il posto giusto. Ti accompagniamo nella scrittura.')}<div class="new-content-cards">${[['confapi','Una notizia CONFAPI','Comunicazioni e aggiornamenti dell’associazione.'],['aniem','Una notizia ANIEM','Notizie, appalti e periodici per l’edilizia.'],['servizi','Un articolo di un servizio','Contenuti per lavoro, credito, formazione e le altre categorie.']].map(([id,t,p])=>`<a href="/admin/articoli/nuovo?channel=${id}"><span class="channel-label ${id}">${labels[id]}</span><h2>${t}</h2><p>${p}</p><strong>Inizia a scrivere ${icon('plus')}</strong></a>`).join('')}</div><div class="metrics"><a class="metric" href="/admin/articoli?status=published"><p>Articoli pubblicati</p><strong>${counts.published||0}</strong><small>Visibili sul sito</small></a><a class="metric" href="/admin/articoli?status=draft"><p>Bozze da completare</p><strong>${counts.draft||0}</strong><small>Visibili solo in redazione</small></a><a class="metric" href="/admin/richieste"><p>Nuove richieste</p><strong>${sql.get("SELECT COUNT(*) n FROM leads WHERE status='Nuova'").n}</strong><small>Dal modulo Contatti</small></a><a class="metric" href="/admin/adesioni"><p>Adesioni da verificare</p><strong>${sql.get("SELECT COUNT(*) n FROM memberships WHERE status<>'Completata'").n}</strong><small>Con PDF e firme</small></a></div><section class="panel"><div class="section-top"><h2>Ultimi articoli modificati</h2><a class="text-link" href="/admin/articoli">Vedi tutti →</a></div>${articleRows(sql.all("SELECT * FROM articles WHERE status<>'trash' ORDER BY updated_at DESC LIMIT 7").map(sql.hydrate))}</section>`, 'dashboard');}
function articles(user,query={}){let clauses=[],params=[];const q=String(query.q||'').slice(0,100),channel=['confapi','aniem','servizi'].includes(query.channel)?query.channel:'',status=['draft','published','trash'].includes(query.status)?query.status:'';if(q){clauses.push('title LIKE ?');params.push('%'+q+'%');}if(channel){clauses.push('channel=?');params.push(channel);}if(status){clauses.push('status=?');params.push(status);}else clauses.push("status<>'trash'");const rows=sql.all('SELECT * FROM articles WHERE '+clauses.join(' AND ')+' ORDER BY updated_at DESC',...params).map(sql.hydrate);return shell(user,'Articoli',`${heading('Tutti gli articoli','Cerca il titolo, scegli il canale oppure riprendi una bozza.','<a class="button" href="/admin/articoli/nuovo">'+icon('plus')+' Scrivi un articolo</a>')}<form class="search-row" method="get"><input type="search" name="q" class="search-field" placeholder="Cerca un titolo…" value="${esc(q)}" aria-label="Cerca un titolo"><select name="channel" class="search-field" aria-label="Canale"><option value="">Tutti i canali</option>${Object.entries(labels).map(([id,l])=>`<option value="${id}" ${channel===id?'selected':''}>${l}</option>`).join('')}</select><select name="status" class="search-field" aria-label="Stato"><option value="">Tutti gli stati</option>${Object.entries(statusNames).map(([id,l])=>`<option value="${id}" ${status===id?'selected':''}>${l}</option>`).join('')}</select><button class="button" type="submit">Cerca</button></form><section class="panel"><p class="count">${rows.length} articoli</p>${rows.length?articleRows(rows):'<div class="empty">Nessun articolo corrisponde ai filtri selezionati.</div>'}</section>`,'articles');}
function editor(user,a,channel){channel=a?.channel||(['confapi','aniem','servizi'].includes(channel)?channel:'confapi');return shell(user,a?'Modifica articolo':'Nuovo articolo',`${heading(a?'Modifica articolo':'Scrivi un nuovo articolo','Tre passaggi: scegli dove pubblicare, scrivi, controlla.','<a class="row-button" href="/admin/articoli">← Tutti gli articoli</a>')}<form id="article-editor" data-id="${esc(a?.id)}" data-version="${a?.version||0}" data-status="${a?.status||'draft'}"><div class="editor-actions"><span class="save-indicator" id="save-indicator">${a?'Tutte le modifiche sono salvate':'Nuovo articolo, non ancora salvato'}</span><div><button type="button" class="button light" data-article-action="draft">${a?.status==='published'?'Ritira in bozza':'Salva bozza'}</button><button type="button" class="button light" data-article-action="preview">Vedi anteprima</button><button type="button" class="button" data-article-action="published">${a?.status==='published'?'Salva e aggiorna il sito':'Pubblica sul sito'} ${icon()}</button></div></div><div class="editor-layout"><div><section class="panel"><div class="number-heading"><span>1</span><div><h2>Dove vuoi pubblicare?</h2><p>Scegli il canale nel quale il lettore troverà l’articolo.</p></div></div><div class="channel-options">${Object.entries(labels).map(([id,l])=>`<label><input type="radio" name="channel" value="${id}" ${channel===id?'checked':''}><strong>${l}</strong><small>${id==='confapi'?'Aggiornamenti dell’associazione':id==='aniem'?'Notizie per le imprese dell’edilizia':'Articoli delle categorie di servizio'}</small></label>`).join('')}</div><fieldset class="service-checkboxes"><legend id="service-legend">Servizi collegati ${channel==='servizi'?'*':'(facoltativi)'}</legend><p id="service-help">${channel==='servizi'?'Scegli almeno un servizio: l’articolo comparirà nella relativa pagina.':'Puoi rendere questa notizia visibile anche nelle pagine dei servizi pertinenti.'}</p><div class="checkbox-grid">${sql.activeServices().map(s=>`<label><input type="checkbox" name="services" value="${esc(s.id)}" ${a?.services.some(x=>x.id===s.id)?'checked':''}>${esc(s.name)}</label>`).join('')}</div></fieldset></section><section class="panel"><div class="number-heading"><span>2</span><div><h2>Scrivi il tuo articolo</h2><p>Usa i pulsanti come in un normale programma di scrittura.</p></div></div><label class="field">Titolo *<input id="article-title" name="title" value="${esc(a?.title)}" required minlength="3" maxlength="220" placeholder="Es. Nuovo incontro per le imprese associate"></label><label class="field">Breve introduzione<textarea name="summary" id="article-summary" maxlength="600" placeholder="Spiega in due o tre righe di cosa parla l’articolo.">${esc(a?.summary)}</textarea></label><label class="field" id="body-label">Testo dell’articolo *</label><div id="quill-toolbar"><span class="ql-formats"><select class="ql-header" aria-label="Stile del paragrafo"><option selected>Testo normale</option><option value="2">Titolo</option><option value="3">Sottotitolo</option></select></span><span class="ql-formats"><button class="ql-bold" aria-label="Grassetto" title="Grassetto"></button><button class="ql-italic" aria-label="Corsivo" title="Corsivo"></button><button class="ql-underline" aria-label="Sottolineato" title="Sottolineato"></button></span><span class="ql-formats"><button class="ql-list" value="bullet" aria-label="Elenco puntato" title="Elenco puntato"></button><button class="ql-list" value="ordered" aria-label="Elenco numerato" title="Elenco numerato"></button></span><span class="ql-formats"><button class="ql-link" aria-label="Inserisci link" title="Inserisci link"></button><button class="ql-clean" aria-label="Rimuovi formattazione" title="Rimuovi formattazione"></button></span><button type="button" class="insert-image-button" data-open-media="inline">${icon('image')} Inserisci immagine</button></div><div id="quill-editor" aria-labelledby="body-label">${a?.html||''}</div><p class="field-help">Per aggiungere una foto nel testo, posiziona il cursore e premi “Inserisci immagine”. Puoi cancellarla con Backspace. Le foto caricate restano disponibili nella libreria.</p></section><section class="panel"><div class="number-heading"><span>3</span><div><h2>Controlla prima di pubblicare</h2><p>Verifica canale, titolo, foto e testo con “Vedi anteprima”. Solo “Pubblica sul sito” rende visibile una nuova bozza.</p></div></div><p class="form-error" role="alert" id="editor-error"></p><div id="article-live-link">${a?.status==='published'?`<a class="text-link" href="/notizie/${esc(a.id)}" target="_blank" rel="noopener">Apri l’articolo sul sito ↗</a>`:''}</div></section></div><aside class="editor-sidebar"><section class="panel"><h2>Immagine di copertina</h2><p>È la foto che compare nell’elenco delle notizie.</p><div id="cover-preview">${a?.image?`<img src="${esc(a.image)}" alt="${esc(a.image_alt)}">`:'<div class="cover-empty">Nessuna immagine scelta</div>'}</div><input type="hidden" id="article-image" value="${esc(a?.image)}"><button type="button" class="button light" data-open-media="cover">${icon('image')} Scegli immagine</button><button type="button" class="row-button" data-remove-cover>Rimuovi copertina</button><label class="field">Descrizione della foto<input id="article-image-alt" value="${esc(a?.image_alt)}" maxlength="250" placeholder="Cosa si vede nella foto?"></label></section><section class="panel"><h2>Visibilità</h2><label class="check"><input type="checkbox" id="article-featured" ${a?.featured?'checked':''}><span>Metti in evidenza nella homepage del canale.</span></label><label class="field">Data della pubblicazione<input id="article-date" type="date" value="${a?.published_at?a.published_at.slice(0,10):''}" max="${new Date().toISOString().slice(0,10)}"></label><p class="field-help">Lascia vuota per utilizzare la data di pubblicazione. Puoi mantenere la data di un articolo d’archivio.</p></section>${a?`<section class="panel"><h2>Versioni precedenti</h2><button type="button" class="row-button" data-show-revisions>Mostra le versioni salvate</button><div id="revisions-list"></div></section><section class="panel"><h2>${a.status==='trash'?'Ripristina l’articolo':'Cestino'}</h2><p>${a.status==='trash'?'Salva come bozza per riportare l’articolo in redazione.':'Puoi togliere l’articolo dal sito e conservarlo nel cestino.'}</p>${a.status!=='trash'?'<button type="button" class="row-button danger-text" data-article-action="trash">Sposta nel cestino</button>':''}</section>`:''}</aside></div></form>`,'articles');}
function immagini(user){const media=sql.all('SELECT * FROM media ORDER BY created_at DESC');return shell(user,'Immagini',`${heading('Le immagini della redazione','Carica una foto una sola volta. Ritrovala qui e riutilizzala negli articoli.','<button class="button" data-open-media="library">'+icon('plus')+' Carica un’immagine</button>')}<div class="media-library media-page">${media.map(m=>`<article class="media-card"><img src="/uploads/${esc(m.filename)}" alt="${esc(m.alt)}"><div><h2>${esc(m.original_name)}</h2><p>${esc(m.alt)}</p><small>${m.width} × ${m.height} pixel · ${Math.round(m.size/1024)} KB</small><button class="row-button" data-edit-media="${esc(m.id)}" data-alt="${esc(m.alt)}">Modifica descrizione</button></div></article>`).join('')||'<div class="empty"><h2>La prima immagine parte da qui.</h2><p>Premi “Carica un’immagine”, scegli la foto e aggiungi una breve descrizione.</p></div>'}</div>`,'media');}
function servizi(user){const list=sql.all('SELECT s.*, (SELECT COUNT(*) FROM article_services j WHERE j.service_id=s.id) count FROM services s ORDER BY position,name');return shell(user,'Categorie dei servizi',`${heading('Le categorie dei servizi','I nomi vengono aggiornati nel menu e negli articoli collegati. Gli indirizzi delle pagine restano stabili.')}<div class="panel">${list.map(s=>`<form class="service-edit-form" data-service-id="${esc(s.id)}"><div><label class="field">Nome del servizio<input name="name" required minlength="3" maxlength="100" value="${esc(s.name)}" ${user.role!=='admin'?'disabled':''}></label><label class="field">Breve descrizione<textarea name="summary" maxlength="500" ${user.role!=='admin'?'disabled':''}>${esc(s.summary)}</textarea></label></div><div><span class="status">${s.count} articoli collegati</span><label class="check"><input type="checkbox" name="active" ${s.active?'checked':''} ${user.role!=='admin'?'disabled':''}>Visibile nel sito</label>${user.role==='admin'?'<button class="button light" type="submit">Salva modifiche</button>':''}<p class="form-error" role="alert"></p></div></form>`).join('')}</div>${user.role==='admin'?`<form class="panel" id="new-service-form"><h2>Aggiungi una categoria di servizio</h2>${field('name','Nome del servizio',{required:true,max:100})}<label class="field">Breve descrizione<textarea name="summary" maxlength="500"></textarea></label><p class="form-error" role="alert"></p><button class="button" type="submit">Aggiungi servizio ${icon('plus')}</button></form>`:'<p>Le categorie possono essere modificate da un amministratore.</p>'}`,'services');}
function richieste(user){const list=sql.all('SELECT * FROM leads ORDER BY created_at DESC');return shell(user,'Richieste di contatto',`${heading('Richieste di contatto','Il destinatario è sempre indicato: CONFAPI Roma oppure ANIEM Lazio.')}<div class="requests-list">${list.map(r=>`<form class="panel lead-edit-form" data-id="${esc(r.id)}"><div class="section-top"><div><span class="channel-label ${r.association}">${r.association==='aniem'?'ANIEM Lazio':'CONFAPI Roma'}</span><h2>${esc(r.company)}</h2><p>${esc(r.name)} · <a href="mailto:${esc(r.email)}">${esc(r.email)}</a>${r.phone?' · '+esc(r.phone):''}</p></div><time>${date(r.created_at)}</time></div><p class="request-message">${esc(r.message)}</p><div class="form-grid"><label class="field">Stato<select name="status">${['Nuova','In lavorazione','Gestita'].map(s=>`<option ${s===r.status?'selected':''}>${s}</option>`).join('')}</select></label><label class="field">Note interne<textarea name="notes" maxlength="5000">${esc(r.notes)}</textarea></label></div><button class="button light" type="submit">Aggiorna richiesta</button><p class="form-error" role="alert"></p></form>`).join('')||'<div class="empty"><h2>Nessuna richiesta ricevuta.</h2><p>Le richieste inviate dalla pagina Contatti compariranno qui.</p></div>'}</div>`,'leads');}
function adesioni(user){const list=sql.all('SELECT * FROM memberships ORDER BY created_at DESC');return shell(user,'Domande di adesione',`${heading('Domande di adesione','Il PDF compilato e firmato è accessibile soltanto alla redazione.')}<div class="panel">${list.length?`<div class="table-wrap"><table><thead><tr><th>Impresa</th><th>Ricevuta il</th><th>PDF</th><th>Stato</th></tr></thead><tbody>${list.map(m=>`<tr><td><strong>${esc(m.company)}</strong><br><a href="mailto:${esc(m.email)}">${esc(m.email)}</a><small>Riferimento ${esc(m.id.slice(0,8))}</small></td><td>${date(m.created_at)}</td><td><a class="button light" href="/admin/adesioni/${esc(m.id)}/pdf">Scarica PDF</a></td><td><select class="search-field" data-membership-status="${esc(m.id)}">${['Ricevuta','In verifica','Completata'].map(s=>`<option ${s===m.status?'selected':''}>${s}</option>`).join('')}</select></td></tr>`).join('')}</tbody></table></div>`:'<div class="empty"><h2>Nessuna domanda ricevuta.</h2><p>Le adesioni inviate dalla pagina Come associarsi compariranno qui con il relativo PDF.</p></div>'}</div>`,'memberships');}
function account(user){return shell(user,'Accessi e account',`${heading('Accessi e account','Ogni persona entra con il proprio account.')}<form id="password-form" class="panel"><h2>Cambia la tua password</h2>${field('current','Password attuale',{type:'password',required:true,autocomplete:'current-password',max:200})}${field('password','Nuova password, almeno 14 caratteri',{type:'password',required:true,autocomplete:'new-password',max:200})}<p class="form-error" role="alert"></p><button class="button" type="submit">Aggiorna password</button></form>${user.role==='admin'?`<section class="panel"><h2>Persone abilitate</h2>${sql.all('SELECT name,email,role FROM users ORDER BY name').map(u=>`<p>${esc(u.name)} · ${esc(u.email)} · ${u.role==='admin'?'Amministratore':'Redattore'}</p>`).join('')}</section><form id="new-user-form" class="panel"><h2>Aggiungi una persona alla redazione</h2>${field('name','Nome e cognome',{required:true,max:100})}${field('email','Email',{type:'email',required:true,max:200})}${field('password','Password iniziale, almeno 14 caratteri',{type:'password',required:true,autocomplete:'new-password',max:200})}<label class="field">Ruolo<select name="role"><option value="editor">Redattore: articoli, immagini e richieste</option><option value="admin">Amministratore: anche categorie e accessi</option></select></label><p class="form-error" role="alert"></p><button class="button" type="submit">Crea l’accesso</button></form>`:''}`,'account');}
module.exports={login,dashboard,articles,editor,immagini,servizi,richieste,adesioni,account};
